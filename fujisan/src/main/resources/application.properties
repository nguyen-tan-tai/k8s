spring.application.name=demo

# ==========================================
# Actuator: Health and Info
# ==========================================
# Expose standard health and info endpoints (metrics & traces are handled by OpenTelemetry Agent)
management.endpoints.web.exposure.include=health,info

# Disable Micrometer default metrics since OpenTelemetry handles it
management.metrics.export.defaults.enabled=false
